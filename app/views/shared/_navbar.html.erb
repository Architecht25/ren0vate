<nav class="navbar navbar-expand-lg navbar-light bg-light px-4 py-          <!-- Séparateur pour mobile -->
          <li class="d-lg-none"><hr class="dropdown-divider"></li>

          <!-- Dropdown utilisateur -->  <div class="container-fluid">
    <!-- Logo -->
    <% if user_signed_in? %>
      <a class="navbar-brand d-flex align-items-center" href="<%= dashboard_path %>">
        <i class="bi bi-house-heart-fill me-2 text-primary-custom"></i>
        <span class="fw-semibold">Ren0vate.primes</span>
      </a>
    <% else %>
      <a class="navbar-brand d-flex align-items-center" href="/">
        <i class="bi bi-house-heart-fill me-2 text-primary-custom"></i>
        <span class="fw-semibold">Ren0vate.primes</span>
      </a>
    <% end %>

    <!-- Bouton hamburger pour mobile -->
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarContent"
            aria-controls="navbarContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <!-- Contenu de la navbar (collapsible) -->
    <div class="collapse navbar-collapse" id="navbarContent">
      <ul class="navbar-nav ms-auto mb-2 mb-lg-0">
        <% if user_signed_in? %>
          <!-- Liens de navigation -->

          <li class="nav-item">
            <%= link_to profile_path, class: "nav-link px-3 d-flex align-items-center" do %>
              <i class="bi bi-person-circle me-2" style="color: #64748b;"></i>
              Mon profil
            <% end %>
          </li>

          <li class="nav-item">
            <%= link_to properties_path, class: "nav-link px-3 d-flex align-items-center" do %>
              <i class="bi bi-house me-2" style="color: #16a34a;"></i> Mes biens
            <% end %>
          </li>
          <li class="nav-item">
            <%= link_to projects_path, class: "nav-link px-3 d-flex align-items-center" do %>
              <i class="bi bi-hammer me-2" style="color: #ea580c;"></i> Mes chantiers
            <% end %>
          </li>
          <li class="nav-item">
            <%= link_to simulations_path, class: "nav-link px-3 d-flex align-items-center" do %>
              <i class="bi bi-calculator me-2" style="color: #2563eb;"></i> Mes simulations
            <% end %>
          </li>
          <li class="nav-item">
            <%= link_to decision_hub_index_path, class: "nav-link px-3 d-flex align-items-center" do %>
              <i class="bi bi-gear-wide-connected me-2" style="color: #28a745;"></i>
              <span>Espace technique</span>
            <% end %>
          </li>

          <li class="nav-item">
            <%= link_to documents_path, class: "nav-link px-3 d-flex align-items-center" do %>
              <i class="bi bi-file-earmark-text me-2" style="color: #7c3aed;"></i> Mes documents
            <% end %>
          </li>
          <li class ="nav-item">
            <%= link_to requests_path, class: "nav-link px-3 d-flex align-items-center" do %>
              <i class="bi bi-list me-2" style="color: #0891b2;"></i> Mes formulaires
            <% end %>
          </li>
          <li class="nav-item">
            <%= link_to request_progresses_path, class: "nav-link px-3 d-flex align-items-center" do %>
              <i class="bi bi-clipboard-check me-2" style="color: #059669;"></i> Suivis de primes
            <% end %>
          </li>
          <li class="nav-item position-relative">
            <%= link_to notifications_path, class: "nav-link px-3 d-flex align-items-center" do %>
              <i class="bi bi-bell me-2" style="color: #dc2626;"></i> Mes notifications
              <%= unread_notifications_badge(current_user) %>
            <% end %>
          </li>

          <!-- Pricing / Upgrade -->
          <li class="nav-item">
            <%= link_to pricing_select_path, class: "nav-link px-3 d-flex align-items-center" do %>
              <i class="bi bi-gem me-2" style="color: #f59e0b;"></i>
              <% if defined?(tier_upgrade_available?) && tier_upgrade_available? %>
                <span class="badge bg-warning text-dark"><%= upgrade_badge_text %></span>
              <% else %>
                <span class="badge bg-primary">Premium</span>
              <% end %>
            <% end %>
          </li>

          <!-- Séparateur pour mobile -->
          <li class="d-lg-none"><hr class="dropdown-divider"></li>

          <!-- Admin Dashboard (si admin) -->
          <% if current_user&.admin? %>
            <li class="nav-item">
              <%= link_to '/admin/dashboard', class: "nav-link px-3 d-flex align-items-center" do %>
                <i class="bi bi-speedometer2 me-2" style="color: #ff6600;"></i>
                <span style="color: #ff6600;">Administration</span>
              <% end %>
            </li>
          <% end %>


          <!-- Déconnexion -->
          <li class="nav-item" data-controller="logout-modal">
            <%= button_to destroy_user_session_path, method: :delete,
                data: { action: "click->logout-modal#show" },
                class: "nav-link text-primary d-flex align-items-center border-0 bg-transparent px-3" do %>
              <i class="bi bi-box-arrow-right me-2" style="color: #ef4444;"></i> Déconnexion
            <% end %>

            <!-- Modal de confirmation -->
            <%= render 'shared/logout_modal' %>
          </li>
        <% else %>
          <li class="nav-item">
            <%= link_to new_user_session_path, class: "btn btn-primary-custom px-4" do %>
              <i class="bi bi-box-arrow-in-right me-2"></i> Connexion
            <% end %>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>

<!-- Style pour la navbar -->
<style>
  .navbar .nav-link:hover {
    color: var(--primary-custom);
  }

  .navbar .dropdown-item:active {
    background-color: var(--primary-custom);
  }

  @media (max-width: 992px) {
    .navbar .nav-link {
      padding: 0.75rem 1rem;
    }
  }
</style>
