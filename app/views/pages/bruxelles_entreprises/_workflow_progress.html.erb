<div class="card border-0 shadow-sm">
  <div class="card-body py-3">
    <div class="row align-items-center">
      <div class="col">
        <div class="progress-workflow d-flex justify-content-between align-items-center">
          <!-- Étape 1: BCE Search -->
          <div class="step-item" data-step="1">
            <div class="step-circle bg-success text-white d-flex align-items-center justify-content-center">
              <i class="fas fa-search"></i>
            </div>
            <div class="step-label mt-2">
              <small class="fw-bold">Identification</small><br>
              <small class="text-muted">Numéro BCE</small>
            </div>
          </div>

          <!-- Connecteur -->
          <div class="step-connector">
            <div class="connector-line bg-secondary"></div>
          </div>

          <!-- Étape 2: Éligibilité -->
          <div class="step-item" data-step="2">
            <div class="step-circle bg-secondary text-white d-flex align-items-center justify-content-center">
              <i class="fas fa-clipboard-check"></i>
            </div>
            <div class="step-label mt-2">
              <small class="fw-bold">Éligibilité</small><br>
              <small class="text-muted">Vérifications</small>
            </div>
          </div>

          <!-- Connecteur -->
          <div class="step-connector">
            <div class="connector-line bg-secondary"></div>
          </div>

          <!-- Étape 3: Calcul -->
          <div class="step-item" data-step="3">
            <div class="step-circle bg-secondary text-white d-flex align-items-center justify-content-center">
              <i class="fas fa-calculator"></i>
            </div>
            <div class="step-label mt-2">
              <small class="fw-bold">Calculs</small><br>
              <small class="text-muted">Estimations</small>
            </div>
          </div>

          <!-- Connecteur -->
          <div class="step-connector">
            <div class="connector-line bg-secondary"></div>
          </div>

          <!-- Étape 4: Résultats -->
          <div class="step-item" data-step="4">
            <div class="step-circle bg-secondary text-white d-flex align-items-center justify-content-center">
              <i class="fas fa-file-alt"></i>
            </div>
            <div class="step-label mt-2">
              <small class="fw-bold">Résultats</small><br>
              <small class="text-muted">Rapport final</small>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>

<style>
.progress-workflow {
  position: relative;
}

.step-item {
  text-align: center;
  position: relative;
  z-index: 2;
}

.step-circle {
  width: 50px;
  height: 50px;
  border-radius: 50%;
  margin: 0 auto;
  font-size: 18px;
  transition: all 0.3s ease;
}

.step-circle.active {
  transform: scale(1.1);
  box-shadow: 0 0 15px rgba(0,123,255,0.3);
}

.step-label {
  min-width: 80px;
}

.step-connector {
  flex: 1;
  position: relative;
  z-index: 1;
}

.connector-line {
  height: 3px;
  width: 100%;
  margin: 0 15px;
  transition: all 0.3s ease;
}

.connector-line.completed {
  background-color: #28a745 !important;
}
</style>
