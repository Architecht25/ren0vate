<% content_for :title, "Documents#{" - #{@property.name}" if @property}" %>

<div class="container-fluid py-4">
  <div class="row">
    <div class="col-12">
      <!-- Header -->
      <div class="d-flex justify-content-between align-items-center mb-4">
        <div>
          <!-- Breadcrumb -->
          <% if @selected_document_type || @selected_phase %>
            <nav aria-label="breadcrumb">
              <ol class="breadcrumb mb-2">
                <li class="breadcrumb-item">
                  <%= link_to documents_path(
                                property_id: @property&.id,
                                project_id: @project&.id
                              ), class: "text-decoration-none" do %>
                    <i class="bi bi-folder2 me-1"></i>Documents
                  <% end %>
                </li>
                <% if @selected_phase && !@selected_document_type %>
                  <li class="breadcrumb-item active" aria-current="page">
                    <%= @selected_phase.icon %> <%= @selected_phase.name %>
                  </li>
                <% elsif @selected_document_type %>
                  <% if @selected_phase %>
                    <li class="breadcrumb-item">
                      <%= link_to documents_path(
                                    property_id: @property&.id,
                                    project_id: @project&.id,
                                    filter_phase: @selected_phase.id
                                  ), class: "text-decoration-none" do %>
                        <%= @selected_phase.icon %> <%= @selected_phase.name %>
                      <% end %>
                    </li>
                  <% end %>
                  <li class="breadcrumb-item active" aria-current="page">
                    <%= @selected_document_type.humanize %>
                  </li>
                <% end %>
              </ol>
            </nav>
          <% end %>
          
          <h1 class="h3 mb-1">
            <i class="bi bi-folder2 text-primary me-2"></i>
            Documents
            <% if @property %>
              - <%= @property.name %>
            <% elsif @project %>
              - <%= @project.name %>
            <% end %>
          </h1>
          <p class="text-muted mb-0">
            <% if @selected_document_type %>
              Documents de type "<%= @selected_document_type.humanize %>" • <%= @documents.count %> document(s)
              <% if @selected_phase %>
                <span class="badge bg-<%= @selected_phase.color %> ms-2"><%= @selected_phase.name %></span>
              <% end %>
            <% elsif @selected_phase %>
              Documents de la <%= @selected_phase.name.downcase %> • <%= @documents.count %> document(s)
            <% elsif params[:type_document].present? %>
              Documents de type "<%= params[:type_document].humanize %>" • <%= @documents.count %> document(s)
            <% else %>
              Gestion de vos documents • <%= @documents.count %> document(s)
            <% end %>
          </p>
        </div>
        <div class="d-flex gap-2">
          <% if @property %>
            <%= link_to documents_phases_dashboard_property_path(@property),
                        class: "btn btn-outline-info btn-sm" do %>
              <i class="bi bi-kanban me-1"></i>Vue phases
            <% end %>
            <%= link_to property_dashboard_path(@property),
                        class: "btn btn-outline-secondary" do %>
              <i class="bi bi-arrow-left me-2"></i>Dashboard bien
            <% end %>
          <% elsif @project %>
            <%= link_to @project, class: "btn btn-outline-secondary" do %>
              <i class="bi bi-arrow-left me-2"></i>Retour projet
            <% end %>
          <% else %>
            <%= link_to dashboard_path, class: "btn btn-outline-secondary" do %>
              <i class="bi bi-arrow-left me-2"></i>Dashboard
            <% end %>
          <% end %>
        </div>
      </div>

      <!-- Filtres et actions -->
      <div class="row mb-4">
        <div class="col-md-8">
          <div class="card border-0 shadow-sm">
            <div class="card-body">
              <div class="row align-items-center">
                <div class="col-md-6">
                  <!-- Filtre par phase - disponible pour tous les contextes -->
                  <% if @phases_data&.any? %>
                    <div class="mb-3 mb-md-0">
                      <label class="form-label small text-muted">Filtrer par phase :</label>
                      <div class="btn-group d-flex" role="group">
                        <%= link_to documents_path(
                              property_id: @property&.id,
                              project_id: @project&.id
                            ),
                                   class: "btn #{'btn-primary' if @selected_phase.nil?} #{'btn-outline-primary' unless @selected_phase.nil?} btn-sm" do %>
                          <i class="bi bi-grid me-1"></i>Toutes
                        <% end %>
                        <% @phases_data.each do |phase_data| %>
                          <% phase = phase_data[:phase] %>
                          <%= link_to documents_path(
                                        property_id: @property&.id,
                                        project_id: @project&.id,
                                        filter_phase: phase.id
                                      ),
                                     class: "btn #{'btn-primary' if @selected_phase == phase} #{'btn-outline-primary' unless @selected_phase == phase} btn-sm" do %>
                            <%= phase.icon %> <%= phase.name.gsub('Phase ', '') %>
                          <% end %>
                        <% end %>
                      </div>
                    </div>
                  <% end %>
                </div>
                <div class="col-md-6 text-end">
                  <!-- Actions rapides -->
                  <div class="btn-group" role="group">
                    <%= link_to new_document_path(
                          property_id: @property&.id,
                          project_id: @project&.id,
                          upload_mode: 'ocr'
                        ),
                        class: "btn btn-info btn-sm" do %>
                      <i class="bi bi-eye me-1"></i>Scan OCR
                    <% end %>
                    <%= link_to new_document_path(
                          property_id: @property&.id,
                          project_id: @project&.id
                        ),
                        class: "btn btn-primary btn-sm" do %>
                      <i class="bi bi-plus-circle me-1"></i>Nouveau document
                    <% end %>
                  </div>
                </div>
              </div>
            </div>
          </div>
        </div>
        <div class="col-md-4">
          <!-- Stats rapides -->
          <div class="card border-0 shadow-sm">
            <div class="card-body">
              <div class="row text-center">
                <div class="col-4">
                  <div class="text-success h5 mb-0"><%= @document_stats[:by_status]['approved'] || 0 %></div>
                  <small class="text-muted">Approuvés</small>
                </div>
                <div class="col-4">
                  <div class="text-warning h5 mb-0"><%= @document_stats[:by_status]['pending'] || 0 %></div>
                  <small class="text-muted">En attente</small>
                </div>
                <div class="col-4">
                  <div class="text-danger h5 mb-0"><%= @document_stats[:by_status]['rejected'] || 0 %></div>
                  <small class="text-muted">Rejetés</small>
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>

      <!-- Vue par phases - affichée dans tous les contextes -->
      <% if @phases_data&.any? %>
        <% if @selected_phase.present? %>
          <!-- Vue détaillée de la phase sélectionnée -->
          <div class="row mb-4">
            <div class="col-12">
              <div class="card border-0 shadow-sm">
                <div class="card-header bg-<%= @selected_phase.color %> text-white">
                  <div class="d-flex align-items-center">
                    <div class="me-3" style="font-size: 2rem;"><%= @selected_phase.icon %></div>
                    <div class="flex-grow-1">
                      <h4 class="mb-1"><%= @selected_phase.name %></h4>
                      <p class="mb-0 opacity-75"><%= @selected_phase.description %></p>
                    </div>
                    <div class="text-end">
                      <%= link_to documents_path(
                                    property_id: @property&.id,
                                    project_id: @project&.id
                                  ),
                                 class: "btn btn-outline-light btn-sm" do %>
                        <i class="bi bi-arrow-left me-1"></i>Toutes les phases
                      <% end %>
                    </div>
                  </div>
                </div>
                
                <div class="card-body">
                  <!-- Types de documents obligatoires -->
                  <% if @selected_phase.required_document_types.any? %>
                    <div class="mb-4">
                      <h6 class="text-danger mb-3">
                        <i class="bi bi-exclamation-triangle me-2"></i>Documents obligatoires
                      </h6>
                      <div class="row">
                        <% @selected_phase.required_document_types.each do |doc_type| %>
                          <% docs_of_type = @documents.select { |d| d.type_document == doc_type } %>
                          <% has_approved = docs_of_type.any? { |d| d.status == 'approved' } %>
                          
                          <div class="col-lg-3 col-md-6 mb-3">
                            <div class="card border-<%= has_approved ? 'success' : 'danger' %> h-100">
                              <div class="card-body text-center">
                                <div class="mb-2">
                                  <i class="bi bi-<%= has_approved ? 'check-circle-fill text-success' : 'exclamation-circle-fill text-danger' %>" style="font-size: 2rem;"></i>
                                </div>
                                <h6 class="card-title">
                                  <%= doc_type.humanize %>
                                  <% if doc_type == 'attestation_entrepreneur' %>
                                    <span class="badge bg-info ms-1">
                                      <i class="bi bi-download me-1"></i>Officiels
                                    </span>
                                  <% end %>
                                </h6>
                                <p class="small text-muted mb-2"><%= docs_of_type.count %> document(s)</p>
                                
                                <div class="d-flex gap-1 justify-content-center">
                                  <% if doc_type == 'attestation_entrepreneur' %>
                                    <%= link_to prime_document_templates_path,
                                               class: "btn btn-outline-info btn-sm" do %>
                                      <i class="bi bi-download me-1"></i>Voir officiels
                                    <% end %>
                                  <% else %>
                                    <%= link_to documents_path(
                                                  property_id: @property&.id,
                                                  project_id: @project&.id,
                                                  type_document: doc_type
                                                ),
                                               class: "btn btn-outline-secondary btn-sm" do %>
                                      <i class="bi bi-eye me-1"></i>Voir
                                    <% end %>
                                  <% end %>
                                  <%= link_to new_document_path(
                                                property_id: @property&.id,
                                                project_id: @project&.id,
                                                type_document: doc_type,
                                                upload_mode: 'ocr'
                                              ),
                                             class: "btn btn-primary btn-sm" do %>
                                    <i class="bi bi-plus me-1"></i>Ajouter
                                  <% end %>
                                </div>
                              </div>
                            </div>
                          </div>
                        <% end %>
                      </div>
                    </div>
                  <% end %>
                  
                  <!-- Types de documents optionnels -->
                  <% if @selected_phase.optional_document_types.any? %>
                    <div class="mb-3">
                      <h6 class="text-info mb-3">
                        <i class="bi bi-info-circle me-2"></i>Documents optionnels
                      </h6>
                      <div class="row">
                        <% @selected_phase.optional_document_types.each do |doc_type| %>
                          <% docs_of_type = @documents.select { |d| d.type_document == doc_type } %>
                          <% has_docs = docs_of_type.any? %>
                          
                          <div class="col-lg-3 col-md-6 mb-3">
                            <div class="card border-<%= has_docs ? 'info' : 'secondary' %> h-100">
                              <div class="card-body text-center">
                                <div class="mb-2">
                                  <i class="bi bi-<%= has_docs ? 'file-earmark-check text-info' : 'file-earmark-plus text-muted' %>" style="font-size: 2rem;"></i>
                                </div>
                                <h6 class="card-title">
                                  <%= doc_type.humanize %>
                                  <% if doc_type == 'attestation_entrepreneur' %>
                                    <span class="badge bg-info ms-1">
                                      <i class="bi bi-download me-1"></i>Officiels
                                    </span>
                                  <% end %>
                                </h6>
                                <p class="small text-muted mb-2"><%= docs_of_type.count %> document(s)</p>
                                
                                <div class="d-flex gap-1 justify-content-center">
                                  <% if doc_type == 'attestation_entrepreneur' %>
                                    <%= link_to prime_document_templates_path,
                                               class: "btn btn-outline-info btn-sm" do %>
                                      <i class="bi bi-download me-1"></i>Voir officiels
                                    <% end %>
                                  <% elsif has_docs %>
                                    <%= link_to documents_path(
                                                  property_id: @property&.id,
                                                  project_id: @project&.id,
                                                  type_document: doc_type
                                                ),
                                               class: "btn btn-outline-info btn-sm" do %>
                                      <i class="bi bi-eye me-1"></i>Voir
                                    <% end %>
                                  <% end %>
                                  <%= link_to new_document_path(
                                                property_id: @property&.id,
                                                project_id: @project&.id,
                                                type_document: doc_type,
                                                upload_mode: 'ocr'
                                              ),
                                             class: "btn btn-outline-secondary btn-sm" do %>
                                    <i class="bi bi-plus me-1"></i>Ajouter
                                  <% end %>
                                </div>
                              </div>
                            </div>
                          </div>
                        <% end %>
                      </div>
                    </div>
                  <% end %>
                </div>
              </div>
            </div>
          </div>
        <% else %>
          <!-- Mini vue des phases -->
          <div class="row mb-4">
            <div class="col-12 mb-3">
              <h5 class="text-muted mb-0">
                <i class="bi bi-kanban me-2"></i>Vue d'ensemble par phases métier
              </h5>
              <small class="text-muted">
                <% if @property %>
                  Progression spécifique pour <%= @property.name %>
                <% else %>
                  Progression générale de vos documents
                <% end %>
              </small>
            </div>
          <% @phases_data.each do |phase_data| %>
            <% phase = phase_data[:phase] %>
            <% docs_in_phase = @documents.select { |d| d.document_phase == phase } %>
            
            <div class="col-lg-3 col-md-6 mb-3">
              <div class="card border-0 shadow-sm h-100">
                <div class="card-body">
                  <div class="d-flex align-items-center mb-2">
                    <div class="me-2" style="font-size: 1.5rem;"><%= phase.icon %></div>
                    <div class="flex-grow-1">
                      <h6 class="card-title mb-0"><%= phase.name.gsub('Phase ', '') %></h6>
                      <small class="text-muted"><%= docs_in_phase.count %> doc(s)</small>
                    </div>
                    <div class="badge bg-<%= phase_data[:completion_percentage] >= 80 ? 'success' : phase_data[:completion_percentage] >= 50 ? 'warning' : 'danger' %>">
                      <%= phase_data[:completion_percentage] %>%
                    </div>
                  </div>
                  
                  <!-- Progress bar -->
                  <div class="progress mb-2" style="height: 4px;">
                    <div class="progress-bar bg-<%= phase_data[:completion_percentage] >= 80 ? 'success' : phase_data[:completion_percentage] >= 50 ? 'warning' : 'danger' %>"
                         style="width: <%= phase_data[:completion_percentage] %>%"></div>
                  </div>
                  
                  <!-- Description de la phase -->
                  <p class="small text-muted mb-2"><%= phase.description %></p>
                  
                  <!-- Documents requis/optionnels -->
                  <div class="small mb-2">
                    <div class="text-muted">
                      <strong>Obligatoires:</strong> <%= phase.required_document_types.count %>
                      | <strong>Optionnels:</strong> <%= phase.optional_document_types.count %>
                    </div>
                  </div>
                  
                  <!-- Actions -->
                  <div class="d-flex justify-content-between">
                    <%= link_to documents_path(
                                  property_id: @property&.id,
                                  project_id: @project&.id,
                                  filter_phase: phase.id
                                ),
                               class: "btn btn-outline-#{phase.color} btn-sm" do %>
                      <i class="bi bi-folder2-open me-1"></i>Voir (<%= docs_in_phase.count %>)
                    <% end %>
                    <% if phase_data[:missing_required]&.any? %>
                      <%= link_to new_document_path(
                                    property_id: @property&.id,
                                    project_id: @project&.id,
                                    type_document: phase_data[:missing_required].first,
                                    upload_mode: 'ocr'
                                  ),
                                 class: "btn btn-primary btn-sm" do %>
                        <i class="bi bi-plus me-1"></i>Ajouter
                      <% end %>
                    <% else %>
                      <%= link_to new_document_path(
                                    property_id: @property&.id,
                                    project_id: @project&.id,
                                    type_document: phase.required_document_types.first
                                  ),
                                 class: "btn btn-outline-secondary btn-sm" do %>
                        <i class="bi bi-plus me-1"></i>Nouveau
                      <% end %>
                    <% end %>
                  </div>
                </div>
              </div>
            </div>
          <% end %>
        </div>
        <% end %>
      <% end %>      <!-- Liste des documents -->
      <% if @documents.any? %>
        <div class="row">
          <% @documents.order(created_at: :desc).each do |document| %>
            <div class="col-lg-4 col-md-6 mb-4">
              <div class="card border-0 shadow-sm h-100">
                <!-- Header de la carte avec phase -->
                <div class="card-header bg-light border-0 d-flex justify-content-between align-items-center">
                  <div class="d-flex align-items-center">
                    <% if document.respond_to?(:document_phase) && document.document_phase %>
                      <span class="me-2" style="font-size: 1.2rem;"><%= document.document_phase.icon %></span>
                      <small class="text-muted"><%= document.document_phase.name %></small>
                    <% else %>
                      <small class="text-muted">Non classé</small>
                    <% end %>
                  </div>
                  <div class="d-flex gap-1">
                    <!-- Badge de priorité -->
                    <% if document.respond_to?(:priority_badge_class) %>
                      <span class="badge bg-<%= document.priority_badge_class %> badge-sm">
                        <%= document.respond_to?(:priority_text) ? document.priority_text : 'Normal' %>
                      </span>
                    <% end %>
                    <!-- Badge de statut -->
                    <span class="badge bg-<%=
                      case document.status
                      when 'approved' then 'success'
                      when 'pending' then 'warning'
                      when 'rejected' then 'danger'
                      else 'secondary'
                      end
                    %> badge-sm">
                      <%=
                        case document.status
                        when 'approved' then '✓ Approuvé'
                        when 'pending' then '⏳ En attente'
                        when 'rejected' then '✗ Rejeté'
                        else 'Non défini'
                        end
                      %>
                    </span>
                  </div>
                </div>

                <div class="card-body">
                  <!-- Type de document -->
                  <h6 class="card-title">
                    <i class="bi bi-file-earmark-text text-primary me-2"></i>
                    <%= document.type_document.humanize %>
                  </h6>

                  <!-- Informations du fichier -->
                  <% if document.file.attached? %>
                    <div class="d-flex align-items-center mb-2">
                      <i class="bi bi-paperclip text-muted me-2"></i>
                      <div class="flex-grow-1">
                        <div class="small fw-medium"><%= document.file.filename %></div>
                        <div class="text-muted small">
                          <%= number_to_human_size(document.file.byte_size) %>
                        </div>
                      </div>
                      <% if document.file.content_type.start_with?('image/') || document.file.content_type == 'application/pdf' %>
                        <button class="btn btn-outline-info btn-sm preview-btn"
                                data-document-id="<%= document.id %>">
                          <i class="bi bi-eye"></i>
                        </button>
                      <% end %>
                    </div>
                  <% end %>

                  <!-- Notes (tronquées) -->
                  <% if document.notes.present? %>
                    <div class="mb-2">
                      <small class="text-muted">Notes:</small>
                      <div class="small text-truncate"><%= document.notes %></div>
                    </div>
                  <% end %>

                  <!-- Suggestions d'amélioration -->
                  <% if document.respond_to?(:improvement_suggestions) %>
                    <% suggestions = document.improvement_suggestions %>
                    <% if suggestions.any? %>
                      <div class="alert alert-info alert-sm mb-2">
                        <i class="bi bi-lightbulb me-1"></i>
                        <small><%= suggestions.first %></small>
                      </div>
                    <% end %>
                  <% end %>

                  <!-- Date de création -->
                  <div class="text-muted small mb-3">
                    <i class="bi bi-calendar3 me-1"></i>
                    Ajouté <%= time_ago_in_words(document.created_at) %>
                  </div>
                </div>

                <!-- Actions -->
                <div class="card-footer bg-transparent border-0">
                  <div class="d-flex gap-2">
                    <%= link_to document, class: "btn btn-outline-primary btn-sm" do %>
                      <i class="bi bi-eye me-1"></i>Voir
                    <% end %>
                    <% if document.file.attached? %>
                      <%= link_to rails_blob_path(document.file, disposition: "attachment"),
                                 class: "btn btn-outline-secondary btn-sm" do %>
                        <i class="bi bi-download me-1"></i>Télécharger
                      <% end %>
                    <% end %>
                    <%= link_to edit_document_path(document),
                               class: "btn btn-outline-warning btn-sm" do %>
                      <i class="bi bi-pencil me-1"></i>Modifier
                    <% end %>
                    <%= link_to document, method: :delete,
                               class: "btn btn-outline-danger btn-sm",
                               confirm: "Êtes-vous sûr de vouloir supprimer ce document ?",
                               form: { style: "display: inline;" } do %>
                      <i class="bi bi-trash me-1"></i>Supprimer
                    <% end %>
                  </div>
                </div>
              </div>
            </div>
          <% end %>
        </div>
      <% else %>
        <!-- État vide -->
        <div class="text-center py-5">
          <div class="mb-4">
            <i class="bi bi-folder2 text-muted" style="font-size: 4rem;"></i>
          </div>
          <h4 class="text-muted mb-3">
            <% if @selected_phase %>
              Aucun document pour la <%= @selected_phase.name.downcase %>
            <% elsif params[:type_document].present? %>
              Aucun document de type "<%= params[:type_document].humanize %>"
            <% else %>
              Aucun document trouvé
            <% end %>
          </h4>
          <p class="text-muted mb-4">
            <% if @selected_phase %>
              Commencez par ajouter les documents requis pour cette phase.
            <% else %>
              Commencez par ajouter votre premier document.
            <% end %>
          </p>
          <div class="d-flex gap-2 justify-content-center">
            <%= link_to new_document_path(
                  property_id: @property&.id,
                  project_id: @project&.id,
                  type_document: @selected_phase&.required_document_types&.first,
                  upload_mode: 'ocr'
                ),
                class: "btn btn-info" do %>
              <i class="bi bi-eye me-2"></i>Scan OCR
            <% end %>
            <%= link_to new_document_path(
                  property_id: @property&.id,
                  project_id: @project&.id,
                  type_document: @selected_phase&.required_document_types&.first
                ),
                class: "btn btn-primary" do %>
              <i class="bi bi-plus-circle me-2"></i>Ajouter un document
            <% end %>
          </div>
        </div>
      <% end %>
    </div>
  </div>
</div>

<!-- Modal de prévisualisation -->
<div class="modal fade" id="previewModal" tabindex="-1" aria-hidden="true">
  <div class="modal-dialog modal-lg">
    <div class="modal-content">
      <div class="modal-header">
        <h5 class="modal-title">Prévisualisation du document</h5>
        <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
      </div>
      <div class="modal-body">
        <div id="preview-content" class="text-center">
          <div class="spinner-border" role="status">
            <span class="visually-hidden">Chargement...</span>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>

<style>
.card {
  transition: all 0.3s ease;
}

.card:hover {
  transform: translateY(-2px);
  box-shadow: 0 4px 15px rgba(0,0,0,0.1) !important;
}

.badge-sm {
  font-size: 0.7rem;
  padding: 0.25em 0.5em;
}

.alert-sm {
  padding: 0.5rem;
  margin-bottom: 0.5rem;
}

.preview-btn:hover {
  transform: scale(1.1);
}
</style>

<script>
document.addEventListener('turbo:load', function() {
  // Gestion de la prévisualisation
  const previewButtons = document.querySelectorAll('.preview-btn');
  const previewModal = new bootstrap.Modal(document.getElementById('previewModal'));
  const previewContent = document.getElementById('preview-content');

  previewButtons.forEach(button => {
    button.addEventListener('click', function() {
      const documentId = this.dataset.documentId;

      previewContent.innerHTML = `
        <div class="spinner-border" role="status">
          <span class="visually-hidden">Chargement...</span>
        </div>
      `;

      previewModal.show();

      fetch(`/documents/${documentId}/preview`)
        .then(response => response.json())
        .then(data => {
          if (data.type === 'image') {
            previewContent.innerHTML = `<img src="${data.url}" class="img-fluid" alt="Prévisualisation">`;
          } else if (data.type === 'pdf') {
            previewContent.innerHTML = `<embed src="${data.url}" type="application/pdf" width="100%" height="600px">`;
          } else {
            previewContent.innerHTML = `<div class="alert alert-warning">${data.message || 'Prévisualisation non disponible'}</div>`;
          }
        })
        .catch(error => {
          previewContent.innerHTML = `<div class="alert alert-danger">Erreur lors du chargement de la prévisualisation</div>`;
        });
    });
  });
});
</script>
