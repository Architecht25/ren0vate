<!-- Isolation toiture -->
<div class="form-check mb-3">
  <%= form.check_box :travaux_toiture, 
                     { class: "form-check-input", checked: form_data[:travaux_toiture] },
                     "true", "" %>
  <%= form.label :travaux_toiture, "Isolation de la toiture", class: "form-check-label fw-semibold" %>
</div>
<div class="travaux-details ms-4 mb-4 <%= 'd-none' unless form_data[:travaux_toiture] %>">
  <div class="row g-2">
    <div class="col-md-6">
      <%= form.label :surface_toiture, "Surface (m²)", class: "form-label" %>
      <%= form.number_field :surface_toiture, 
                           class: "form-control",
                           min: 0, step: 1 %>
    </div>
    <div class="col-md-6">
      <%= form.label :type_toiture, "Méthode", class: "form-label" %>
      <%= form.select :type_toiture, 
                     options_for_select([
                       ['-- Sélectionnez --', ''],
                       ['Par l\'intérieur', 'interieur'],
                       ['Par l\'extérieur', 'exterieur']
                     ]),
                     {},
                     { class: "form-select" } %>
    </div>
  </div>
</div>

<!-- Isolation murs extérieurs -->
<div class="form-check mb-3">
  <%= form.check_box :travaux_murs, 
                     { class: "form-check-input", checked: form_data[:travaux_murs] },
                     "true", "" %>
  <%= form.label :travaux_murs, "Isolation des murs extérieurs", class: "form-check-label fw-semibold" %>
</div>
<div class="travaux-details ms-4 mb-4 <%= 'd-none' unless form_data[:travaux_murs] %>">
  <div class="row g-2">
    <div class="col-md-6">
      <%= form.label :surface_murs, "Surface (m²)", class: "form-label" %>
      <%= form.number_field :surface_murs, 
                           class: "form-control",
                           min: 0, step: 1 %>
    </div>
    <div class="col-md-6">
      <%= form.label :type_murs, "Méthode", class: "form-label" %>
      <%= form.select :type_murs, 
                     options_for_select([
                       ['-- Sélectionnez --', ''],
                       ['Murs creux', 'creux'],
                       ['Par l\'intérieur', 'interieur'],
                       ['Par l\'extérieur', 'exterieur']
                     ]),
                     {},
                     { class: "form-select" } %>
    </div>
  </div>
</div>

<!-- Vitrage -->
<div class="form-check mb-3">
  <%= form.check_box :travaux_vitrage, 
                     { class: "form-check-input", checked: form_data[:travaux_vitrage] },
                     "true", "" %>
  <%= form.label :travaux_vitrage, "Remplacement du vitrage", class: "form-check-label fw-semibold" %>
</div>
<div class="travaux-details ms-4 mb-4 <%= 'd-none' unless form_data[:travaux_vitrage] %>">
  <div class="row g-2">
    <div class="col-md-6">
      <%= form.label :surface_vitrage, "Surface (m²)", class: "form-label" %>
      <%= form.number_field :surface_vitrage, 
                           class: "form-control",
                           min: 0, step: 1 %>
    </div>
  </div>
</div>

<!-- Isolation du sol -->
<div class="form-check mb-3">
  <%= form.check_box :travaux_sol, 
                     { class: "form-check-input", checked: form_data[:travaux_sol] },
                     "true", "" %>
  <%= form.label :travaux_sol, "Isolation du sol ou des caves", class: "form-check-label fw-semibold" %>
</div>
<div class="travaux-details ms-4 mb-4 <%= 'd-none' unless form_data[:travaux_sol] %>">
  <div class="row g-2">
    <div class="col-md-6">
      <%= form.label :surface_sol, "Surface (m²)", class: "form-label" %>
      <%= form.number_field :surface_sol, 
                           class: "form-control",
                           min: 0, step: 1 %>
    </div>
  </div>
</div>

<!-- Chauffage (chaudière ou PAC) -->
<div class="form-check mb-3">
  <%= form.check_box :travaux_chauffage, 
                     { class: "form-check-input", checked: form_data[:travaux_chauffage] },
                     "true", "" %>
  <%= form.label :travaux_chauffage, "Installation d'une pompe à chaleur et chauffe-eau thermodynamique", class: "form-check-label fw-semibold" %>
</div>
<div class="travaux-details ms-4 mb-4 <%= 'd-none' unless form_data[:travaux_chauffage] %>">
  <div class="row g-2">
    <div class="col-md-6">
      <%= form.label :type_chauffage, "Type de système", class: "form-label" %>
      <%= form.select :type_chauffage, 
                     options_for_select([
                       ['-- Sélectionnez --', ''],
                       ['Pompe à chaleur géothermique', 'pac-geothermique'],
                       ['Pompe à chaleur air/eau', 'pac-air-eau'],
                       ['Pompe à chaleur air/air', 'pac-air-air'],
                       ['Pompe à chaleur hybride', 'hybride'],
                       ['Boiler thermodynamique', 'boiler']
                     ]),
                     {},
                     { class: "form-select" } %>
    </div>
  </div>
</div>

<script>
// JavaScript pour afficher/masquer les détails des travaux
document.addEventListener('DOMContentLoaded', function() {
  const checkboxes = document.querySelectorAll('.form-check-input[id^="travaux_"]');
  
  checkboxes.forEach(checkbox => {
    checkbox.addEventListener('change', function() {
      const detailsDiv = this.closest('.form-check').nextElementSibling;
      if (detailsDiv && detailsDiv.classList.contains('travaux-details')) {
        if (this.checked) {
          detailsDiv.classList.remove('d-none');
        } else {
          detailsDiv.classList.add('d-none');
        }
      }
    });
  });
});
</script>
