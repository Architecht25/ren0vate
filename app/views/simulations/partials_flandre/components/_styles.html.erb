<!-- Styles pour les cartes améliorées -->
<style>
  .drop-shadow {
    text-shadow: 2px 2px 4px rgba(0,0,0,0.5);
  }

  .card-premium {
    transition: all 0.3s ease;
    border: 2px solid var(--ren0vate-border) !important;
    box-shadow: var(--ren0vate-shadow) !important;
  }

  .card-premium:hover {
    transform: translateY(-5px);
    box-shadow: var(--ren0vate-shadow-lg) !important;
    border-color: var(--ren0vate-primary) !important;
  }

  /* Bordures renforcées pour toutes les cartes */
  .card {
    border: 1px solid var(--ren0vate-border) !important;
    box-shadow: 0 2px 8px rgba(0,0,0,0.06) !important;
  }

  .card:hover {
    border-color: var(--ren0vate-primary-light) !important;
    box-shadow: var(--ren0vate-shadow) !important;
  }

  /* Cartes spéciales avec bordures distinctives */
  .card-outline-primary {
    border: 2px solid var(--ren0vate-primary) !important;
    box-shadow: 0 3px 10px rgba(var(--ren0vate-primary-rgb), 0.15) !important;
  }

  .card-outline-success {
    border: 2px solid var(--ren0vate-success) !important;
    box-shadow: 0 3px 10px rgba(var(--ren0vate-success-rgb), 0.15) !important;
  }

  .card-outline-warning {
    border: 2px solid var(--ren0vate-warning) !important;
    box-shadow: 0 3px 10px rgba(var(--ren0vate-accent-rgb), 0.15) !important;
  }

  .card-outline-info {
    border: 2px solid var(--ren0vate-info) !important;
    box-shadow: 0 3px 10px rgba(59,130,246,0.15) !important;
  }

  .image-overlay {
    background: linear-gradient(135deg, rgba(0,0,0,0.4), rgba(0,0,0,0.2));
    backdrop-filter: blur(1px);
  }

  @keyframes spin {
    from { transform: rotate(0deg); }
    to   { transform: rotate(360deg); }
  }

  .spin {
    display: inline-block;
    animation: spin 1s linear infinite;
  }
</style>
