<!-- ÉTAPE 1: Test d'éligibilité Bruxelles Entreprise -->
<div class="card border-0 shadow-sm mb-4 h-100" id="eligibility-step">
  <div class="card-body p-4 d-flex flex-column">
    <h5 class="card-title text-primary-custom mb-3">
      <i class="bi bi-shield-check me-2"></i>Étape 1 : Test d'éligibilité (Entreprise Bruxelles)
    </h5>

    <div id="eligibility-status">
      <% if @simulation.respond_to?(:eligible) && @simulation.eligible? %>
        <div class="alert alert-success d-flex align-items-center" role="alert">
          <i class="bi bi-check-circle-fill me-3 fs-4"></i>
          <div>
            <h6 class="alert-heading mb-1">✅ Votre entreprise est éligible !</h6>
            <p class="mb-0">Vous remplissez les conditions pour bénéficier des aides aux entreprises de Bruxelles.</p>
            <% if @simulation.respond_to?(:category_description) && @simulation.category_description.present? %>
              <small class="text-muted">Détails : <%= @simulation.category_description %></small>
            <% end %>
          </div>
        </div>
      <% elsif @simulation.respond_to?(:eligible) && !@simulation.eligible? %>
        <div class="alert alert-danger d-flex align-items-center" role="alert">
          <i class="bi bi-x-circle-fill me-3 fs-4"></i>
          <div>
            <h6 class="alert-heading mb-1">❌ Votre entreprise n'est pas éligible</h6>
            <p class="mb-0">Certaines conditions ne sont pas remplies pour les aides aux entreprises Bruxelles.</p>
            <% if @simulation.respond_to?(:ineligibility_reason) && @simulation.ineligibility_reason.present? %>
              <small class="text-muted">Raison : <%= @simulation.ineligibility_reason %></small>
            <% end %>
          </div>
        </div>
      <% else %>
        <div class="alert alert-info d-flex align-items-center" role="alert">
          <i class="bi bi-hourglass-split me-3 fs-4"></i>
          <div>
            <h6 class="alert-heading mb-1">🔄 Test d'éligibilité prêt</h6>
            <p class="mb-0">Cliquez sur le bouton ci-dessous pour vérifier l'éligibilité de votre entreprise selon les critères Bruxelles.</p>
            <small class="text-muted d-block mt-1">
              Vérification automatique basée sur vos données d'entreprise (localisation, secteur, BCE, etc.)
            </small>
          </div>
        </div>
      <% end %>
    </div>

    <% unless @simulation.respond_to?(:eligible) && @simulation.eligible? %>
      <div class="mt-3">
        <button id="check-eligibility-btn" class="btn btn-primary-custom">
          <i class="bi bi-shield-check me-2"></i>Vérifier l'éligibilité Entreprise Bruxelles
        </button>
      </div>
    <% end %>
  </div>
</div>
