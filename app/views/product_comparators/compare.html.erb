<%# product_comparators/compare.html.erb — Page comparaison détaillée %>

<% content_for :title, "Comparateur #{Product::CATEGORIES[@category]} - Ren0vate" %>

<div class="container py-4">
  <%= render 'product_comparators/compare_content',
        result:      @result,
        project:     @project,
        category:    @category,
        subcategory: @subcategory,
        priorities:  @priorities %>
</div>
